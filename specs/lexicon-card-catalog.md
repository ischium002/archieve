# Spec：设定词条馆（Lexicon）—— 目录柜卡片盒

2026-07-18 定稿。对应 ROADMAP「世界观辞典（Glossary）」条目，坐骨拍板改为卡片盒形态，馆名定为 Lexicon。

## 已拍板的决定

1. **原 world card 去留**：Universe 页保留前三张综述卡（时代背景 / 魔法体系 / 科技背景），恶魔族群、HEAD-LESS、天使族群三张词条卡迁入词条馆，原位换一张入口卡。
2. **翻卡形态**：堆叠露顶边（每张卡只露顶端一行：词条名 + 编号），不用网格平铺。
3. **入口**：nav 加 Lexicon 项 + Universe 页入口卡，两个都要。
4. **文案分工**：迁移的三张卡沿用坐骨原文；新词条列表和文案由坐骨提供，壳先用 `draft: true` 占位词条撑结构。
5. **彩蛋**：页面初载抽屉是关着的（只见抽屉面板 + 铜牌标签），点一下拉开。深链直达词条时跳过关抽屉状态。

## 页面结构（`lexicon/index.html` + `lexicon/lexicon-data.js`）

三个状态：

- **闭合态**：目录柜抽屉面板，铜色标签框（用 `--md` 金棕）+ 拉手，点击拉开。
- **翻卡态**：抽屉内部俯视。按分类分组，每组一个突出的分隔签（tab 位置随分类序号左中右轮换），组内卡片竖排、每张只露顶边。桌面 hover 微抬，触屏直接点。
- **抽出态**：点某张卡，放大到屏幕中央 overlay。仿目录卡：词条名下一条红头线、正文横线格（行高与格线同步 28px）、右上角 mono 编号、分类小印章（旋转、`--stamp` 红）、编者注（灰斜体 ✎ 前缀）、参见互链（点击换卡）、底部中央穿杆圆孔。关闭：× / 点背景 / Esc。

## 数据 schema（`lexicon-data.js`）

```js
LEX_CATS    // 有序数组 [{ key, label }]，顺序 = 分隔签顺序
LEX_ENTRIES // [{ id, term, sub?, cat, def, note?, see?, draft? }]
```

- `id` 是锚点路由：`lexicon/#<id>`，起了就别改。全站术语从别处链过来就用这个。
- 编号 `LEX-001` 按数组下标自动生成，**含 draft 占位**——填完文案把 `draft` 去掉即上架，编号不动。缺号 = 待归档，页脚有一句解释（这是特性不是 bug）。
- `see` 指向 draft 词条时渲染为纯文本 +「（待归档）」，不成死链。
- 没有非 draft 词条的分类不渲染分隔签。

## 路由

- `#<id>` 直达：打开即抽屉展开（无动画）+ 该卡抽出。
- 抽卡写 hash，关卡清 hash（replaceState），hashchange 统一驱动，前进后退可用。

## 移动端（断点 600，沿用站内约定）

- 堆叠列表天然竖排，不用变形。
- 抽出态卡片接近全宽，overlay 自身可滚，body 锁滚动。
- 全程点击驱动，无 hover 依赖。

## 主站改动（index.html）

- nav 加 `<a class="nav-link" href="./lexicon/">Lexicon</a>`（在 Timeline 旁）。
- 三张词条卡替换为一张可点击的入口卡（整卡 onclick 跳 `./lexicon/`）。
- 大改前备份：`index.html.backup-pre-lexicon`。

## 待坐骨

- 词条列表 + 文案（现有 draft 占位：热冲突、三区、魔科学、曲速与跃迁、禁忌咒语——尤其禁忌咒语涉及 Dul，是否上架、露多少你定）。
- 入口卡和页头小字文案照例是拟稿，随意改。
- 分类名（纪年 / 疆域 / 学科 / 族群 / 契约）是临时分法，改 `LEX_CATS` 即可。
